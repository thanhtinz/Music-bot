name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest

    # The storage adapters are the one thing a fake cannot vouch for: a typo in
    # a statement or a parameter in the wrong position looks fine to a double
    # and fails on the first real write. Without this the Postgres tests skip.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: musicbot_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/musicbot_test

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # The same font the runtime image installs. Without it the CJK card tests
      # cannot tell a broken renderer from a bare runner, so they skip — and a
      # title regression would reach production green.
      - name: Install CJK fonts
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends fonts-noto-cjk

      - name: Format check
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      # Rendering the cards end to end catches canvas regressions that unit
      # tests on individual primitives would miss.
      - name: Render canvas previews
        run: npm run preview:canvas
